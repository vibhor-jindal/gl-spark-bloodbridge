<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.globallogic.bloodbridge</groupId>
    <artifactId>bloodbridge-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>BloodBridge Parent</name>
    <description>Emergency Blood Donation &amp; Donor Matching Platform — multi-module microservices project</description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-cloud.version>2023.0.1</spring-cloud.version>
    </properties>

    <!--
      Modules are added incrementally, one per commit, matching the sprint's
      daily delivery plan (see root README.md "Delivery Timeline"):
        Day 1 -> eureka-server, api-gateway              (this commit)
        Day 2 -> auth-service
        Day 2-3 -> donor-service
        Day 3 -> request-service
        Day 4 -> matching-service
        Day 4-5 -> notification-service
        Day 5-6 -> inventory-service
        Day 6 -> analytics-service, rewards-service
        Day 7 -> frontend
    -->
    <modules>
        <module>eureka-server</module>
        <module>api-gateway</module>
        <module>auth-service</module>
        <module>donor-service</module>
        <module>request-service</module>
        <module>matching-service</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
